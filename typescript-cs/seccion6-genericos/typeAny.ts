//Al tratar de usar las variables cuando ponemos el punto después del nombre de la variable
//el editor nos arroja las posibles funciones especificas que se pueden aplicar a cada variable
const nombre:string ='Gabriel'
const numero:number = 3
//Sin embargo cuando declaramos el tipo any, el editor no nos arroja ninguna función relacionada con el tipo de variable que 
//estemos usando
let idk:any = 'string'
